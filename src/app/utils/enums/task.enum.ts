export enum TaskStatus {
    TODO = 'TODO',
    PROGRESS = 'PROGRESS',
    REVIEW = 'REVIEW',
    DONE = 'DONE',
}

export enum TaskPriority {
    LOW = 1,
    NORMAL = 2,
    HIGH = 3,
    URGENT = 4,
}
